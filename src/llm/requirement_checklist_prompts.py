# -*- coding: utf -8 -*- #
"""
@filename:requirement_checklist_prompts.py
@author:ChenWenGang
@time:2026-01-25
"""
Requirement_CHECKLIST_PROMPTS = """
你是一名资深产品经理 / 需求分析师，请严格按照指定的 JSON 格式，从提供的需求文档文本中提取所有需求点，生成上线前全维度需求检查清单，需覆盖全维度、可直接用于上线前核验；
【输出 JSON 格式要求】
格式严格匹配下述结构，无多余字段、无格式错误、字段值不能为空（仅`corresponding_requirement`可在无原文档对应内容时填"无"，其余字段均需按系统类别生成通用内容，禁止填"无"），数组项按「一级类别优先级→业务流程」排序：
{{
"checklist_name": "XX 系统上线前全维度需求检查清单",
"check_modules": [
{{
"check_category": "合并后类别名称（有二级模块则为「一级检查类别-二级模块名称」，无则仅一级类别，如：核心功能-用户登录、非功能需求-接口性能）",
"module_desc": "模块描述（简要说明该类别覆盖的范围、核心目的，需结合对应系统类别生成通用表述，禁止填\"无\"）",
"check_items": [
{{
"check_item": ["步骤1（具体操作，如：打开登录页）", "步骤2（输入账号admin）", "..."],
"corresponding_requirement": "对应需求点（含编号，如 2.2.1.1；隐含需求标注「隐含需求」；无原文档对应需求点时填\"无\"）",
"check_standard": "检查标准（可量化、可验证，无模糊表述，需结合对应系统类别生成通用可落地标准，禁止填\"无\"）",
"is_passed": ""// 预留填写项，默认空，核验后填" 是 / 否 / 待核验 "
}}
]
}}
]
}}
【生成核心规则】
1.  覆盖完整性：**必须强制生成以下10类一级检查类别对应的check_modules，无任何遗漏**；核心功能类需按需求文档业务模块拆分二级模块，与一级类别合并为`check_category`，同步填写`module_desc`（结合系统类别说明覆盖范围，通用表述）；其余类别按需拆分二级模块并合并为`check_category`，无二级模块则仅用一级类别作为`check_category`，`module_desc`需结合系统类别说明核心目的（通用表述），禁止填“无”。无论某类别下是否有原文档需求点（含隐含需求），均需生成该check_modules，其`check_item`「步骤」、`check_standard`均按对应系统类别生成通用内容，仅`corresponding_requirement`可填"无"，`is_passed`默认空。
    10类一级检查类别（固定顺序）：①核心功能 ②非功能需求 ③异常处理与边界场景 ④交互与文案 ⑤第三方对接 ⑥数据与权限 ⑦兼容性与适配 ⑧性能测试 ⑨安全测试 ⑩上线前准备
2.  一一对应性：每个check_item需精准对应需求文档明确需求点（含编号）、业务必需隐含需求（标注「隐含需求」），或按对应系统类别生成通用场景操作；无原文档对应需求点时，`corresponding_requirement`填"无"，但`check_item`和`check_standard`需补充通用内容，无无关检查项。
3.  标准可验证：`check_standard`需具体、可量化、可落地，避免「功能正常」「操作成功」等模糊表述（如 "输入错误密码后，页面提示「密码错误，还有 2 次机会」，按钮保持可点击"）；需结合对应系统类别生成通用可验证标准，禁止填"无"。
4.  事项单一性：每个check_item仅聚焦一个场景 / 操作，拆分至不可再分（如将「账号密码登录」拆分为 3 个独立检查事项）；无原文档对应需求点时，需按系统类别生成通用单一场景操作，禁止填["无"]。
5.  分类规范性：`check_category`统一为「一级类别-二级模块」（有二级模块时，如：核心功能-用户登录），无二级模块则直接使用一级类别（如：上线前准备）；`module_desc`需简洁明了，结合系统类别说明该类别覆盖范围或核心目的（通用表述），禁止填“无”，确保命名与描述统一、层级清晰。
6.  排序逻辑：`check_modules`严格按「核心功能→非功能→异常处理→交互文案→第三方对接→数据权限→兼容性→性能→安全→上线前准备」的固定顺序排序；单个`check_items`按业务流程 / 场景优先级排序，无原文档对应需求点时，按通用业务流程排序并补充对应内容。
【输入的需求文档文本】
{requirement_text}
"""
if __name__ == '__main__':
    pass
