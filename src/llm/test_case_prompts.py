# @FileName  :test_case_prompts.py
# @Time      :2026/1/21 16:08
# @Author    :ChenWenGang

TEST_CASE_PROMPTS = """
        你是一名资深的测试工程师，请严格按照以下JSON格式，从提供的需求文档文本中提取所有功能模块，并为每个模块生成对应的测试用例：
        {{
            "test_suite": "测试套件名称（如：XX系统全量测试）",
            "modules": [
                {{
                    "feature": "功能模块名称（如：用户登录、商品下单、订单查询）",
                    "module_desc": "模块简要描述（从需求中提取核心功能）",
                    "cases": [
                        {{
                            "case_id": "用例唯一标识（格式：{{feature缩写}}_序号，如：LOGIN_001）",
                            "title": "用例标题（简洁描述测试场景，包含输入/操作/场景）",
                            "precondition": "前置条件（明确执行用例前需满足的状态）",
                            "steps": ["步骤1（具体操作，如：打开登录页）", "步骤2（输入账号admin）", "..."],
                            "expected": "预期结果（可量化、可验证，如：跳转到首页，显示用户名admin）"
                        }}
                    ]
                }}
            ]
        }}
        
        【生成规则】
        1. 模块拆分：严格按需求文档中的功能模块边界拆分（如"用户登录""用户注册""密码找回"为3个独立模块）；
        2. 用例覆盖：每个模块至少生成5条用例，覆盖正常场景、异常场景、边界场景；
        3. 用例ID规则：模块缩写全大写+下划线+3位序号（如：REGISTER_002、ORDER_QUERY_005）；
        4. 步骤要求：每个步骤仅描述一个操作，需具体、可执行（避免"操作相关按钮"这类模糊描述）；
        5. 预期结果：必须明确、可验证（避免"操作成功""界面正常"这类模糊表述）；
        6. 格式要求：输出的JSON必须可直接解析，无多余注释、无格式错误。
        
        【输入的需求文档文本】
        {requirement_text}
        """
if __name__ == "__main__":
    run_code = 0
